# ======================================
# Crowd Detection & Risk Analysis ONLY
# (Face recognition handled by teammate)
# ======================================

# Computer vision library
# Used to read video files, extract frames, resize images, and draw heatmaps
opencv-python==4.9.0.80

# Numerical computation library
# Used for density maps, grid calculations, and crowd statistics
numpy==1.26.4

# Scientific utilities
# Used for smoothing heatmaps and basic signal processing
scipy==1.11.4

# Deep learning framework
# Required to load and run pre-trained crowd models
torch==2.1.2

# PyTorch vision utilities
# Used for image normalization and tensor conversion
torchvision==0.16.2

# Model library access
# Used to load lightweight pre-trained segmentation / crowd models
timm==0.9.12

# YOLO framework (secondary verification only)
# Used for optional person detection in high-risk zones
ultralytics==8.1.2

# Progress visualization
# Shows progress while processing long videos
tqdm==4.66.1

# Visualization library
# Used to generate and save crowd heatmaps
matplotlib==3.8.2
